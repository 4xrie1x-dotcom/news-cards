# 뉴스 카드뉴스 자동화 — 작업 규칙

## 프로젝트 한 줄
매일 밤 그날의 뉴스를 한국 고등학생이 읽을 수 있게 요약해
인스타그램 카드뉴스 PNG로 자동 생성하는 파이썬 시스템.
웹사이트 아님. 앱 아님. 최종 산출물은 `output/` 안의 이미지 파일.

## 개발자에 대해
- 한국 고등학생. IB CAS Project로 진행.
- 해본 것: Python 기초, HTML/CSS, GitHub Pages, 공공데이터 API, 데이터 분석, 디자인
- 처음인 것: 백엔드, DB, 환경변수·API 키 관리, cron/GitHub Actions, React 등 프레임워크
- 위 항목은 "처음 듣는다"를 전제로 설명할 것.

## 작업 방식 (반드시)
- 한 번에 한 단계만. "전체 만들기" 금지
- 파일당 100줄 이내, 기능별로 분리
- 모든 함수에 한글 주석
- try/except로 에러 처리. 실패 이유는 한글로 print
- 요청한 것만 만든다. 미리 앞서가지 않는다
- 단계가 끝나면 git commit
- 코드 설명 요청이 오면 한 줄씩 성실히 설명한다

## 절대 규칙 (타협 불가)
- 기사 본문을 파일이나 DB에 저장 금지. 메모리에서 요약만 뽑고 즉시 폐기
- 저장하는 것: 요약문 + 원문 링크 + 메타데이터만
- 모든 게시물에 "AI 요약" 표시 + 출처 명시
- 기사에 실린 보도사진은 절대 그대로 사용하지 않는다. 인물 사진은 위키미디어 등
  저작권 프리 소스에서 자동으로 조달한다
- 언론사의 정치성향을 코드에 하드코딩 금지
- API 키는 .env에서만 읽는다. .env는 절대 커밋하지 않는다 (레포 public)
- 유료 API·유료 호스팅·유료 폰트·도메인 제안 금지. 전부 무료
- 셀레니움 등 비공식 인스타 자동화 금지 (약관 위반)

## 대상 독자: 고등학생 (중학생 아님)
- 어휘를 낮추지 않는다. 유치하면 역효과
- "무슨 일인가"보다 "왜 이게 뉴스인가"에 무게를 둔다
- 전문용어는 쓰고 용어 카드로 보충한다
- 톤: 담백한 문어체. "~해요" 금지. 설명조·훈계조 금지. 이모지 최소
- 문장 40~45자 이내

## 중립성 (요약 프롬프트에 항상 포함)
- 의견·평가·추측 금지
- "논란이 되고 있다", "우려된다" 같은 표현 금지
- 주어를 명시한다. "비판받았다" → "야당은 ~라고 비판했다"
- 수동태 대신 능동태
- 발언 자체가 사건인 경우(막말, 공개 비방 등)는 그대로 싣는다
- 진행 중인 논쟁을 다룰 때 기사에 상대측 반응이 있으면 함께 싣는다
- 없는 반론을 억지로 만들지 않는다
- 발언 요약보다 "왜 지금 이 발언이 문제가 되는가"에 무게를 둔다

## 기술 스택 (고정)
- 수집: 구글뉴스 RSS
- 리다이렉트 해제: googlenewsdecoder (구글뉴스 RSS 링크를 실제 언론사 주소로 변환)
- 본문 추출: trafilatura
- 요약: Gemini API Flash-Lite (무료 키)
- 카드 생성: Pillow
- 인물 사진: 위키미디어 커먼즈 API (자동 검색·다운로드, 무료)
- 상황 사진: Pexels API (무료, 즉시 발급)
- 자동 실행: GitHub Actions (public 레포)
- 저장: JSON 파일. DB 없음

설치: `pip install trafilatura feedparser google-genai pillow googlenewsdecoder`

## 다시 제안하지 말 것 (검토 후 폐기됨)
웹 플랫폼·댓글 서비스 / Next.js·Supabase / 데이터베이스 /
빅카인즈·네이버 검색·딥서치·GNews·NewsAPI / newspaper3k /
난이도 슬라이더 / 언론사 논조 비교 뷰 / 브라우저 조작 자동화

## 수동 개입 지점 (자동화하지 말 것)
- 오늘 무엇을 올릴지 최종 선택
- AI 요약문 검수·수정  ← CAS 성찰의 원천. 절대 제거 금지
- 발행 여부 결정

자동으로 남기는 것: 실행 로그(logs/{날짜}.log, pipeline_log.py가 기록).
시작·종료 시각과 소요 시간, 후보 수집 건수(필터별 제외 건수 포함),
다중 소재 방송으로 건너뛴 기사(제목·이유), 최종 선택 기사(제목·언론사·URL),
생성된 카드 장수, 사진 조달 결과(위키미디어/Pexels 성공·실패와 최종 결정
단계), 실패 시 어느 단계에서 왜 실패했는지를 담는다. 파이프라인 각 단계가
이미 print하는 내용을 화면과 파일에 동시에 남기는 방식(tee)이라 별도 포맷
없이 실행 그대로 기록된다.
AI 원본 요약과 수정한 최종본 둘 다 남기는 것은 아직 미구현 (수동 개입
지점이 아직 없어서 "수정한 최종본"이 생기지 않음 — 발행 워크플로가
생기면 함께 구현).

## 카드 규칙 (v6, 워터마크 카드 없이 hook+본문만 발행)
- **발행 카드는 hook + 본문뿐이다. 워터마크 카드는 만들지 않는다.**
  총 최대 7장(hook 1 + 본문 최대 6). 워터마크가 없어지며 본문 여유가
  5장→6장으로 늘었다
- 1장: 후킹, 사진 배경
- 2장부터: 본문. **what(사실관계)을 최우선으로 채우고(최대 4장), why(배경)는
  최소 2장을 보장하며 지면이 남으면 최대 3장까지 늘어난다.**
- 카드당 문장 1개가 기본, 문장이 짧으면 2개까지 허용. 문장 2개가 넘거나
  안전영역을 넘으면 무조건 분리
- 정보(특히 대립 구도의 양측 입장)는 줄이지 않는다. 문장을 짧게 써서
  개수로 조정하고, 그래도 넘치면 장수를 늘리거나(최대 7장까지) why를 줄인다
- terms(용어)와 question(질문)은 카드로 만들지 않는다. **워터마크 카드가
  없어서 원래 그 카드가 담당하던 것(CTA "이 사안, 어떻게 보세요? / 저장하고
  팔로우까지", 계정 핸들, 출처, "AI 요약" 표시, 사진 출처)까지 전부
  `caption.py`가 캡션 하나로 담당한다.** 카드 지면 제약이 없어져서 캡션
  글자수 제한도 없다
- `watermark_card.py`는 당장 안 쓰지만 지우지 않고 남겨뒀다(나중을 위해).
  다만 `MAIN_CTA_TEXT`/`SECONDARY_LINE` 상수는 `caption.py`가 그대로
  import해서 재사용한다 — CTA 문구를 두 곳에 따로 적어두지 않기 위해서다
- **강조 마크업(`{중괄호}`)은 쓰지 않는다.** 본문은 전부 동일한 색
  (#16181C)으로 렌더링한다. `prompts.py`가 더 이상 `{ }` 표시를 지시하지
  않고, `body_card.py`도 관련 렌더링 로직을 제거했다(`EMPHASIS_COLOR`
  상수 자체는 나중을 위해 card_config.py에 남겨둠)
- AI가 사건 복잡도에 따라 what/why 각 장수를 결정한다

(hook 1장만 발행 → 워터마크까지 포함한 여러 장 구조로 복원 → 워터마크만
다시 뺀 지금 구조, 이렇게 세 번 바뀌었다. `body_card.py`/`watermark_card.py`
파일 자체는 이 과정 내내 지운 적 없이 계속 유지됐다.)

## 디자인 규격 (v4, 확정)

### 캔버스·안전 여백
```
캔버스     1080 × 1350
좌우 여백   90px
상단 여백   150px
하단 여백   160px
```
인스타 피드 탐색 탭 썸네일은 정중앙 1080×1080으로 크롭되어 위아래 각 135px가
잘려 보인다. 제목·본문 등 핵심 텍스트는 반드시 안전영역 안에 배치할 것.
카드번호·핸들처럼 없어도 무방한 요소만 여백 바깥쪽에 걸쳐도 됨.

위 여백(상단 150px·하단 160px)은 2장부터(본문)와 마지막(워터마크) 카드에
쓴다. hook 카드(1장)는 사진/검정 영역 자체가 동적으로 바뀌어서 이 값을
쓰지 않고 별도 여백을 쓴다 — 아래 1장 항목 참고.

### 1장 (후킹) — 사진 배경, 사진/검정 동적 분할
캔버스를 사진 영역(위)과 고정 검정 영역(아래)으로 나누되, 경계는 고정값이
아니라 **실제 제목의 줄 수·폰트 크기로 매번 다시 계산**한다
(`card_config.hook_black_area_height`). 제목이 짧으면 사진 영역이 커지고
(최대 999px), 길면 검정 영역이 커진다(최대 609px, = 3줄·108px 기준).
```
사진 영역   위쪽, 동적 계산 (741~999px 사이)
            비율 유지하며 채우고 중앙 크롭, 1.5배 넘게 확대해야 하면
            photo_quality.py가 사진 조달 단계에서 이미 걸러냄
경계        150px 그라데이션(검정 투명→불투명)으로 사진/검정을 자연스럽게 연결
검정 영역   아래쪽, 동적 계산 (351~609px 사이). 인스타 탐색 탭 크롭(135px)을
            항상 포함해서 계산 — 제목·날짜가 크롭 구간에 걸치지 않게 함
좌상단      로고 이미지 (logo_card_white.png, 세로 48px, 비율 유지)
검정 영역 안  제목  ExtraBold 108px 흰색, 최대 3줄, 행간 1.2 (안 맞으면 96px→84px로 축소)
              날짜  Regular 26px 보조색
```

### 2장부터 (본문) — 흰 배경
```
배경      #FFFFFF
본문      ExtraBold 76px, #16181C, 행간 1.35 (안 맞으면 68px→60px로 축소)
          세로 중앙 정렬 / 가로 왼쪽 정렬. 강조 마크업은 쓰지 않는다(전부 동일 색)
카드번호  우상단 26px 보조색 (예: 3/7 — 최대 7장 기준)
핸들      하단 26px 보조색, 구분선 아래
```

### 마지막 카드 (워터마크 + CTA) — 지금은 안 만듦
`watermark_card.py`에 코드는 남아 있지만 `card_deck.render_all_cards()`가
더 이상 이 카드를 만들지 않는다. 아래는 그 코드가 정의하는 스펙(참고용)이고,
**실제로 지금 어디에 어떻게 나오는지는 아래 "출처 표기" 섹션을 봐야 한다**
(전부 캡션으로 옮겨감).
```
이 사안, 어떻게 보세요?      ExtraBold 68px, #FFFFFF
저장하고 팔로우까지          Regular 40px, #C7CBD1

@dailypolitics_kr            Regular 34px, 보조색
출처 {언론사} · AI 요약      Regular 30px, 보조색
```

### 필수 구현: 자동 폰트 축소
텍스트 길이를 측정해 안전영역(가용폭 900px, 지정된 줄 수)을 넘으면 폰트 크기를
단계적으로 줄이는 fallback 로직을 반드시 넣을 것(`text_fit.fit_body_text`).
hook 제목: 108→96→84px. 본문: 76→68→60px. 예외적으로 긴 문장이 나와도
깨지지 않아야 한다.

### 색
```
1장 (사진 배경)
  제목        #FFFFFF
  날짜·로고   #8B9099 / 흰색(로고는 이미지 자체가 흰색)

2장~ (흰 배경)
  배경        #FFFFFF
  본문        #16181C  (강조 마크업 없이 전부 이 색 하나로 통일)
  보조        #8B9099  (카드번호, 핸들, 날짜)
  구분선      #E5E5E5

마지막 (워터마크, 지금은 안 만듦 — 코드·색 정의만 남아있음)
  배경        #16181C
  CTA 메인    #FFFFFF
  CTA 보조    #C7CBD1  (MUTED_COLOR보다 밝게 해서 어두운 배경에서도 잘 보이게 함)
  계정·출처   #8B9099
```
`EMPHASIS_COLOR`(#E03A2F, card_config.py) 자체는 남겨뒀지만 현재 어디서도
쓰지 않는다 — 강조 마크업 기능을 껐다가 나중에 다시 켤 수 있어 정의만 유지.

### 폰트 — Pretendard
```
1장 제목              ExtraBold 800   108px(→96→84)   행간 1.2
본문(2장~)            ExtraBold 800    76px(→68→60)   행간 1.35
워터마크 CTA 메인      ExtraBold 800    68px
워터마크 CTA 보조      Regular  400     40px
워터마크 계정·출처     Regular  400   34px / 30px
보조(번호·핸들·날짜)    Regular  400     26px
```
필요 파일: `fonts/Pretendard-ExtraBold.otf`, `fonts/Pretendard-Regular.otf` — 2개면
충분하다 ("Light" 등 다른 굵기는 쓰지 않는다).

### 강조 마크업 — 제거됨
한때 AI 요약 JSON의 `what`/`why` 문자열 안에 `{ }`로 강조 구절을 표시하고
본문 카드에서 강조색(#E03A2F)으로 렌더링하는 기능이 있었으나, 카드 구조를
hook 1장으로 단순화했다가 다시 여러 장 구조로 되돌리는 과정에서 함께
제거했다. `prompts.py`는 더 이상 `{ }` 마크업을 지시하지 않고, `body_card.py`는
받은 텍스트에서 `{`/`}`가 섞여 있어도 그냥 제거만 하고(혹시 남아있어도 화면에
안 보이게) 전부 동일한 본문 색으로 그린다.

## 결정 사항 (판단 근거 — 코드만 보고도 이유를 알 수 있도록 기록)

### 대립 구도 기사의 인물 사진
발언 공방처럼 인물 2명 이상이 언급되는 기사에서, 그중 일부만 사진 조달에
성공해도 **별도 로직 없이 그대로 발행한다.**
- 이유: 사진은 1장에만 쓰이고 본문은 텍스트라서, 이미지 하나가 만드는 편향
  여지는 제한적이라고 판단했다.
- 사후 대응: 발행 로그로 모니터링한다. 실제로 문제가 되는 사례가 나오면
  그때 대응하며, 이 판단 자체를 CAS 성찰 소재로 다룬다.

### Pexels 상황 사진의 키워드 우선순위
Pexels로 상황 사진을 검색할 때 **사물·정물 키워드를 우선하고, 인물·군중
키워드는 후순위로 둔다.**
- 이유: Pexels는 국가 특정 필터가 약해서 "national assembly" 같은 키워드로
  검색하면 한국이 아닌 다른 나라 사진이 섞여 나올 수 있다. 인물·군중 사진은
  국가 특정이 특히 어려우므로 우선순위를 낮춰 국가 오매칭을 완화한다.
- 건물·랜드마크 계열 키워드는 지역명을 포함해 검색한다
  (예: "Korea national assembly").

## 계정명 (확정)
- 표시 이름: 데일리정치
- 핸들: @dailypolitics_kr
- `card_config.py`에 `ACCOUNT_NAME`(표시 이름)과 `ACCOUNT_HANDLE`(핸들)을
  분리해뒀다. 카드 이미지 위 계정 표시(워터마크 카드, 각 본문 카드 하단 —
  팔로우 유도가 목적이라 실제 핸들이 자연스러움)는 `ACCOUNT_HANDLE`을,
  캡션 해시태그처럼 이름 형태로 읽혀야 하는 자리는 `ACCOUNT_NAME`을 쓴다.
  **여전히 계정명을 코드 여러 곳에 직접 하드코딩하지 않고 이 두 변수로만
  관리한다.**

## 로고 (확정, 파일 3종)
워드마크: DAILYPOLITICS (Teko 계열 압축 그로테스크 산세리프, 대문자, 자간
타이트, 한 줄, 장식 요소 없음)
```
assets/logo_profile.png      1080×1080, 배경 #16181C, 텍스트 흰색
                              → 인스타 프로필 사진용 (코드에서 안 씀)

assets/logo_card_dark.png    어두운 텍스트 버전
                              → 밝은(흰) 배경 카드 위에 얹을 때 사용

assets/logo_card_white.png   흰 텍스트 + 투명 배경
                              → 어두운 배경(사진/워터마크 카드) 위에 얹을 때 사용
                              투명 처리 Pillow로 검증 완료(RGBA, 불투명
                              픽셀 전부 순백색) — 재생성 불필요
```
hook 카드(1장) 좌상단에 `logo_card_white.png`를 세로 40~60px로 삽입한다
(hook_card.py). 사진 배경이라 어두운 톤이 많아 흰색 버전을 쓴다.

## 후킹 제목 원칙
- 정책명·법안명을 그대로 쓰지 않는다
  나쁜 예: "다중생활시설 건축기준 개정 추진"
  좋은 예: "국토부, 고시원에 책상 대신 욕조 넣자"
- 가능하면 발언을 직접 인용한 형태로 만든다
- 제목은 자극적으로, 본문은 사실만. 이 분리를 지킨다

## 출처 표기 (v6)
- 이모지 없음. 워터마크 카드가 없어서 CTA·계정 핸들까지 포함해 캡션
  하나에 다 담는다(`caption.py`). 실제 순서:
  ```
  {hook}

  {what+why 이어붙인 줄글}

  —
  알아두기
  {용어1}은/는 {뜻풀이}
  {용어2}은/는 {뜻풀이}
  출처  {언론사}
  AI 요약
  사진  {저작자}, {라이선스}      ← hook 배경으로 실제 사진 썼을 때만

  이 사안, 어떻게 보세요?
  {question}
  저장하고 팔로우까지
  @dailypolitics_kr

  #뉴스요약 #시사상식 #고등학생 #카드뉴스 #데일리정치
  ```
- 사진 출처 줄은 hook 배경 종류에 따라 형식이 다르다:
  위키미디어 인물 사진은 `사진  {저작자}, {라이선스}`(예: `국민의힘TV, CC BY 3.0`,
  `assets/people/{인물명}/*.json`에서 읽음), Pexels 상황 사진은
  `사진  Pexels ({촬영자})`(API로 받아오면 Pexels 표시가 공식 정책상
  필수라 확인 후 추가함, 2026-08-16), 텍스트 전용 fallback이면 표시 안 함
- 캡션 글자수 제한 없음 — 카드 지면 제약이 없어서 필요한 만큼 쓴다
- "AI 요약" 표시와 출처 표기는 절대 빼지 않는다. "원문 링크는 프로필에"
  문구는 카드에도 캡션에도 넣지 않는다 (완전 삭제)

## 출력 구조
output/{날짜}/{기사번호}/card_N.png, caption.txt, data.json
logs/{날짜}.log

## 진행 순서
0 CLAUDE.md·.gitignore → 1 collect.py → 2 extract.py → 3 summarize.py
→ 4 render.py → 5 caption.py → 6 main.py → 7 GitHub Actions → 8 로그 축적